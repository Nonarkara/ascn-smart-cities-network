{
  "name": "ascn-smart-cities-network",
  "version": "2.0.0",
  "private": true,
  "description": "ASCN V2 Progress Observatory: a lightweight analytics workbench for ASEAN Smart Cities Network reports, city projects, and evidence.",
  "scripts": {
    "extract:data": "python3 scripts/extract_ascn_data.py",
    "build": "node scripts/build-static.mjs",
    "dev": "npm run build && npx wrangler pages dev dist --compatibility-date=2026-06-20",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name ascn-smart-cities-network --branch main"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
